{
  "wizard": {
    "id": "complete_job_v1",
    "version": "1.0.0",
    "name": "Complete Job",
    "capability": "jobs.complete",
    "permissions": {"required_roles": ["field_worker", "supervisor", "admin"]},
    "metadata": {"category": "jobs", "interfaces": ["chat", "mobile", "tablet", "voice"]},
    "offline": {"enabled": true, "sync_priority": "critical", "conflict_handling": "prompt_user"},
    "steps": [
      {
        "id": "job",
        "title": "Select job",
        "fields": [
          {"id": "job_id", "label": "Job", "type": "text", "required": true}
        ]
      },
      {
        "id": "completion",
        "title": "Completion details",
        "fields": [
          {"id": "completed_at", "label": "Completed at", "type": "text", "required": true},
          {"id": "checklist_complete", "label": "Checklist complete", "type": "boolean", "required": true},
          {"id": "actual_duration_minutes", "label": "Duration in minutes", "type": "number", "required": true, "min": 1}
        ]
      },
      {
        "id": "evidence",
        "title": "Evidence and exceptions",
        "fields": [
          {"id": "evidence_attachment_ids", "label": "Evidence attachments", "type": "array", "required": false},
          {"id": "damage_reported", "label": "Damage reported", "type": "boolean", "required": true},
          {"id": "exception_notes", "label": "Exception notes", "type": "text", "required": false, "max": 1000}
        ]
      },
      {
        "id": "customer_outcome",
        "title": "Customer outcome",
        "fields": [
          {"id": "customer_signature", "label": "Customer signature", "type": "text", "required": false},
          {"id": "request_invoice", "label": "Create invoice next", "type": "boolean", "required": true}
        ]
      }
    ]
  }
}
