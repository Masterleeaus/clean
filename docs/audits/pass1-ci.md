# Pass 1 CI

The branch includes `.github/workflows/pass1-baseline.yml` to run Composer strict validation, install PHP dependencies without application scripts, execute the baseline audit and architecture tests, and validate the npm lockfile through a clean install.
