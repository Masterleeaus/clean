# Pass 1 Review Base

A temporary review-base branch is required because the current repository `main` branch does not share the preserved imported application history. The review base exists only to trigger CI against the isolated upgrade branch and must not replace `main` automatically.
