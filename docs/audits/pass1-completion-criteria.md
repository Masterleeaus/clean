# Pass 1 Completion Criteria

Pass 1 is complete only when Composer strict validation, source baseline generation, both architecture tests and npm lockfile installation succeed in CI. Until then, the branch remains in Pass 1.
