# WorkCore Quarantined Components

The following source areas were deliberately excluded from runtime autoload because they are incomplete, donor-derived, host-specific, or conflict with the canonical Meetup/Titan architecture.

## Exclusion rules

- Legacy `App\Extensions\WorkCore` root provider
- Donor and IntegratedSources runtime
- Incomplete Rewind, Intelligence, native AI, and Expansion systems
- MagicAI-specific host adapters and policies
- Unauthorised module HTTP routes/controllers
- Donor `pm_*` managed-property/accommodation implementation remains quarantined; its accepted concepts were rebuilt in canonical Premises and Accommodation runtime modules
- Broken vertical runtime except the self-contained trade-compliance gate

Copied canonical PHP/runtime files: 448

Quarantined files from System/: 457
