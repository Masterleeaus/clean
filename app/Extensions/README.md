# MagicAI Extensions

Expanded extension packages imported from the Titan Zero project sources. The canonical Titan Zero Chatbot package is retained as `Chatbot`. See `/EXTENSIONS_IMPORT_MANIFEST.json` for provenance, hashes, versions, deduplication and validation.
