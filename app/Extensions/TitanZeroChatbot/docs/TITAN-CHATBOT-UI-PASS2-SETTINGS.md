# Titan Chatbot UI Pass 2 — Functional Settings

Adds a functional gear panel for BYO AI providers, encrypted local vault, privacy routing, offline sync, WorkCore status, device capabilities, notifications, appearance, accessibility and diagnostics.

Provider API keys are encrypted through the existing WorkCore AES-256-GCM device vault. Plaintext keys are not persisted by the settings runtime.
