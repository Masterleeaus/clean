name: Inventory extension platform

on:
  push:
    branches:
      - agent/documentation-reconciliation
    paths:
      - '.github/workflows/inventory-extension-platform.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/documentation-reconciliation
          fetch-depth: 0

      - name: Generate extension platform inventory
        run: python3 scripts/docs/inventory_extension_platform.py

      - name: Remove one-time workflow
        run: rm -f .github/workflows/inventory-extension-platform.yml

      - name: Commit inventory
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: inventory extension platform and lifecycle"
          git push origin HEAD:agent/documentation-reconciliation
