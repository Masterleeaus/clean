@extends('layouts.app') @section('title','Invoices') @section('titan-money-title','Invoices') @section('titan-money-subtitle','Canonical financial documents') @section('content') @include('titanmoney::partials.header')<main class="p-6"><div class="max-w-7xl mx-auto space-y-4"><div class="flex justify-between"><form class="flex gap-2"><input name="q" value="{{ request('q') }}" class="border rounded-lg px-3 py-2" placeholder="Search"><select name="status" class="border rounded-lg px-3 py-2"><option value="">All statuses</option>@foreach(\App\Domains\TitanMoney\Enums\InvoiceStatus::cases() as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ ucwords(str_replace('_',' ',$s->value)) }}</option>@endforeach</select><button class="px-3 py-2 bg-gray-900 text-white rounded-lg">Filter</button></form><a class="px-4 py-2 bg-purple-600 text-white rounded-lg" href="{{ route('titanmoney.invoices.create') }}">New invoice</a></div><div class="bg-white border rounded-xl overflow-hidden"><table class="w-full text-sm"><thead class="bg-gray-50"><tr><th class="p-3 text-left">Number</th><th class="p-3 text-left">Customer</th><th class="p-3">Due</th><th class="p-3 text-right">Total</th><th class="p-3 text-right">Balance</th><th class="p-3">Status</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr class="border-t"><td class="p-3"><a class="font-bold text-purple-700" href="{{ route('titanmoney.invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td class="p-3">{{ $invoice->customer->name }}</td><td class="p-3 text-center">{{ optional($invoice->due_date)->format('d M Y') }}</td><td class="p-3 text-right">{{ formatMoneyMinor($invoice->total_minor, $invoice->currency_code) }}</td><td class="p-3 text-right">{{ formatMoneyMinor($invoice->balance_due_minor, $invoice->currency_code) }}</td><td class="p-3 text-center">{{ ucwords(str_replace('_',' ',$invoice->status->value)) }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">No invoices.</td></tr>@endforelse</tbody></table></div>{{ $invoices->links() }}</div></main>@endsection